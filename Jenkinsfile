@Library('MySharedLib') _

pipeline {
    agent any
    stages {
        stage("Library"){
            steps{
             echo "Hello world triggered pipe ${GIT_COMMIT}"
                }
            
        }
    }
}
