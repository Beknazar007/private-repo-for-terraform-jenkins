@Library('MySharedLib') _

pipeline {
    agent any
    stages {
        stage("Library"){
            steps{
                script{
                alik()
                }
                }
            
        }
    }
}
