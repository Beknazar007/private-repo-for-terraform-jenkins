pipeline {
    agent any
    environment { 
        CC = 'clang'
    }
    stages {
        stage('Example') {
            environment { 
                AN_ACCESS_KEY = credentials('docker-hub-password') 
            }
            steps {
                sh 'printenv'
                sh '$BUILD_NUMBER'
            }
        }
    }
}